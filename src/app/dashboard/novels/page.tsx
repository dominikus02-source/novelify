'use client';

// Note: page is 'use client' so metadata is set via root layout template

import { MyNovelsPage } from '@/components/novelify/pages';

export default function NovelsPage() {
  return <MyNovelsPage />;
}
